# 可重复性保障体系 — Reproducibility Framework

> **目录**: `shared/reproducibility/` | **版本**: v1.0.0 | **更新日期**: 2026-06-26

---

## 1. 概述

可重复性保障体系确保 MSRA Pipeline 的分析结果可以被独立复现。核心理念：**可复现 ≠ 完全一致**，关键是关键结论的一致性。

---

## 2. 结果复现验证

### 2.1 验证方式

自动重跑分析代码 3 次，检查关键输出的一致性：

```python
from reproducibility_check import ReproducibilityChecker

checker = ReproducibilityChecker(
    script_path="analysis/main_analysis.py",
    n_runs=3,
    output_dir="reproducibility/"
)
report = checker.run()
```

### 2.2 可接受差异范围

| 指标类型 | 可接受偏差 | 示例 |
|---------|-----------|------|
| 点估计 (OR/HR/β) | ±0.005 | OR=1.50 允许 1.495-1.505 |
| 置信区间 | ±0.01 | 95% CI 边界 ±0.01 |
| P 值 | 结论性一致 | p=0.048 vs p=0.052 → 需标记 |
| 分类结果 | 完全一致 | AIC/BIC 差异 < 1 |
| 图表布局 | 视觉一致 | 坐标/标签/颜色一致 |

### 2.3 一致性等级

| 等级 | 标准 | 操作 |
|------|------|------|
| ✅ 完全一致 | 所有参数 3 次完全一致 | 无需处理 |
| ✅ 一致 | 差异在可接受范围内 | 报告差异，可继续 |
| ⚠️ 轻微警告 | 1-2 个参数超范围但结论不变 | 标记并在报告中说明 |
| ❌ 不一致 | 关键结论变化 | **阻断：必须排查** |

---

## 3. Pipeline 审计追踪器

### 3.1 设计理念

基于 ehrapy CohortTracker (Nature Medicine 2024) 的设计理念，追踪数据清洗的每一步操作。

### 3.2 使用方式

```python
from pipeline_auditor import PipelineAuditor

auditor = PipelineAuditor(raw_data, name="队列清洗",
                          metadata={"研究": "XXX 疗效分析"})

# 记录每步操作
df = df[df["age"] >= 18]
auditor.record_filter("age >= 18", df_before, df, "排除未成年")

df = df.dropna(subset=["bmi"])
auditor.record_dropna(["bmi"], df_before, df)

# 生成审计报告
auditor.save_report("audit_report.md")
```

### 3.3 审计报告内容

- 操作步骤列表（含原数据→新数据映射）
- 每步的行数变化
- 累计数据损失比例
- 可视化数据流图

---

## 4. 集成到质量门闸

Stage 3.5（结果质量门闸）中包含复现验证检查：

```
□ RQ-05: 代码可复现 — 关键结果在 3 次重跑后是否一致？
```

---

## 5. 失败处理流程

```
复现验证失败
    │
    ├── 检查代码随机种子
    │     ├── 缺少 set.seed() → 添加后重试
    │     └── 已有种子 → 继续排查
    │
    ├── 检查是否涉及 Bootstrap/MCMC
    │     ├── 是 → 增大运行次数到 10-100 次
    │     └── 否 → 继续排查
    │
    └── 通知 Orchestrator
          └── 决定：修正代码 / 接受差异 / 阻断
```

---

## 6. 何时需要复现验证

### 必须验证

| 场景 | 原因 |
|------|------|
| 代码包含随机数生成 | 不同种子结果不同 |
| Bootstrap 重采样 | 每次抽样不同 |
| MCMC 采样 | 链间变异 |
| 机器学习模型 | 训练/测试集随机拆分 |
| 大型数据集 (n>10,000) | 数值精度可能受影响 |

### 可跳过

| 场景 | 原因 |
|------|------|
| 纯描述性统计 | 结果确定 |
| 确定性分析 (t 检验) | 结果确定 |
| 已有固定种子 | 可完全复现 |

---

## 7. 相关文件

| 文件 | 说明 |
|------|------|
| `shared/reproducibility/reproducibility_guide.md` | 复现验证指南 |
| `shared/reproducibility/reproducibility_check.py` | Python 复现检查器 |
| `shared/reproducibility/reproducibility_check.R` | R 复现检查器 |
| `shared/reproducibility/pipeline_auditor.py` | 清洗审计追踪器 |
| `shared/reproducibility/reproducibility_integration.py` | Pipeline 集成 |
| `shared/reproducibility/reproducibility_report_generator.R` | R 报告生成器 |