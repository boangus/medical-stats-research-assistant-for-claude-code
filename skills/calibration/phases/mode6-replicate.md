# Mode 6: 复现验证 (Replicate Verification) 🆕

> 来源：从 `skills/calibration/SKILL.md` L796-889 抽取。保留原文完整性，语义等价。

> **🔴[MANDATORY] replicate**: [MANDATORY] 确认复现结果后记录

> 借鉴 LLM-Assisted Replication (arXiv:2602.18453) 的"文本解读→代码生成→执行→差异分析"迭代循环。
> 核心思路：从已发表论文中自动提取统计结果，MSRA 独立生成分析代码并执行，比对两者差异。
> 这是一种比金标准对比更严格的验证——不仅检查结果是否一致，还检查过程是否可复现。

### 工作流程

```
Phase R1: 论文文本解读 → 提取统计方法、数据描述、报告结果
  │ 输入: 论文 PDF/HTML
  │ 输出: 结构化统计摘要 (方法+数据描述+报告结果)
  │ 🔴 提取失败 → 🛑STOP 标记"无法提取"
  ▼
Phase R2: 独立代码生成 → 基于提取的方法描述生成 R/Python 代码
  │ 输入: 结构化统计摘要
  │ 输出: 可执行分析代码
  │ 🔴 代码生成失败 → 🛑STOP 标记"代码生成错误"
  ▼
Phase R3: 执行与结果提取 → 运行代码，提取关键统计量
  │ 输入: 分析代码 + (公开数据集 or 合成数据)
  │ 输出: 独立分析结果
  │ 🔴 执行失败 → 🛑STOP 标记"执行错误"，进入自愈循环(最多3轮)
  ▼
Phase R4: 差异分析 → 比对论文报告结果 vs MSRA 独立结果
  │ 输入: 论文结果 + MSRA 结果
  │ 输出: 差异报告 (数值偏差 + 方法差异 + 结论差异)
  │ 🔴 差异超过阈值 → 标记差异类型，记录到校准数据库
  ▼
Phase R5: 迭代修正 → 基于差异分析调整代码，重新执行
  │ 最多迭代 3 轮
  │ 每轮记录: 修正内容 + 差异变化
  │ 🔴 3轮后仍差异显著 → 标记"不可复现"，记录原因
```

### 差异判定阈值

| 维度 | 一致 | 可接受差异 | 显著差异 |
|------|------|-----------|---------|
| 效应量 | 偏差 < 1% | 偏差 1-5% | 偏差 > 5% |
| p 值 | 同数量级 | 差1个数量级 | 差2+个数量级 |
| 结论(显著/不显著) | 一致 | — | 不一致(最严重) |
| 方法 | 完全一致 | 等价方法(如 Welch's t vs t) | 不同方法 |

### 复现验证输出

```
╔══════════════════════════════════════════════════╗
║          MSRA 复现验证报告                        ║
╠══════════════════════════════════════════════════╣
║ 论文: [标题/DOI]                                  ║
║ 提取分析数: N                                     ║
║ 成功复现: M (XX%)                                 ║
║                                                   ║
║ ┌─────────── 差异摘要 ───────────────┐            ║
║ │ 完全一致:       XX/N = XX%         │            ║
║ │ 可接受差异:     XX/N = XX%         │            ║
║ │ 显著差异:       XX/N = XX%         │            ║
║ │ 不可复现:       XX/N = XX%         │            ║
║ └─────────────────────────────────────┘            ║
║                                                   ║
║ 显著差异详情:                                      ║
║ #1: [方法] 论文 OR=2.3, p=0.01                    ║
║     MSRA OR=1.8, p=0.08 → 结论不一致              ║
║     差异类型: 方法差异(Cox vs Logistic)             ║
║     可能原因: 协变量选择不同                        ║
║                                                   ║
║ 改进建议:                                          ║
║ - [基于差异分析的改进方向]                          ║
╚══════════════════════════════════════════════════╝
```

### 复现验证异常处理

| 触发条件 | 一线处理 | 仍失败兜底 |
|---------|---------|-----------|
| 论文方法描述不完整 | 从Methods节提取关键词，匹配methods_catalog.md中的标准方法名，标注"推断" | 标记为"方法不明确"，跳过该分析 |
| 数据集不可获取 | 使用论文中报告的描述性统计构造合成数据 | 标记为"数据不可获取"，仅对比方法选择 |
| 代码执行环境差异 | 使用 Docker 容器标准化环境 | 记录环境差异，在报告中标注 |
| 论文结果本身有误(已知勘误) | 检索勘误信息，使用修正后结果 | 标注"原文可能有误"，不纳入校准 |
| 迭代3轮仍无法复现 | 记录所有尝试和差异，分析根本原因 | 标记为"不可复现"，记录到校准数据库供后续分析 |

### 与标准校准的关系

复现验证是标准校准的补充：
- **标准校准**: 金标准 vs MSRA 输出 → 检查"结果是否正确"
- **复现验证**: 论文描述 → MSRA 独立执行 → 检查"过程是否可复现"

两者共同构成完整的质量保证体系。复现验证的差异记录也纳入 calibration_db.json，
source 标记为 "replication_verification"。
