# ============================================================
# MSRA 开发依赖
# 安装: pip install -r requirements-dev.txt
# 或:   ./install.sh --dev / .\install.ps1 -Dev
# ============================================================

# ── 测试 ─────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0
pytest-asyncio>=0.21

# ── 配置解析 (variable_standardization YAML 支持) ────────────
pyyaml>=6.0

# ── 代码质量 ─────────────────────────────────────────────────
ruff>=0.4

# ── 类型检查 (可选) ──────────────────────────────────────────
# mypy>=1.0
# pyright>=1.1
