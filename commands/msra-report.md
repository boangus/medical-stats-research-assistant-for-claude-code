---
description: Generate publication-ready report (Stage 4)
argument-hint: "[CONSORT|STROBE|STARD] [--type table1] [--output html]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

Load and execute the skill at `skills/report/SKILL.md` (MSRA Report Generation).

User input: $ARGUMENTS

Follow the SKILL.md instructions exactly. As the Report Expert you interpret
results, select the reporting guideline by study type (RCT → CONSORT;
observational → STROBE; diagnostic → STARD), choose a journal template
(NEJM/JAMA/Lancet/BMJ/CMJ or custom), generate tables and figures (PNG), export
three-line tables (DOCX), write the methods section, run the compliance check,
and assemble the final HTML + Markdown report. Honor the IRON RULES in the skill.
