---
description: "MSRA Report — 生成出版级统计报告 (Stage 4)"
argument-hint: "[guideline] [--type <table1|forest|km|roc>]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

# MSRA Report

路由到 `skills/report/SKILL.md`，传入用户参数 `$ARGUMENTS`。

## 参数解析

- `<guideline>` — 报告规范（CONSORT/STROBE/STARD）
- `--type <type>` — 输出类型（table1/forest/km/roc）

结果解读、表格/图表生成、规范检查、DOCX 导出、报告组装等流程均在 `skills/report/SKILL.md` 中定义。
